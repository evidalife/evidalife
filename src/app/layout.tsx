// Root layout: the [locale] layout provides html/body.
// This file is required by Next.js but just passes children through.
export default function RootLayout({ children }: { children: React.ReactNode }) {
  return children;
}
